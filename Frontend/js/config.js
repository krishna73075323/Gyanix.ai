// Gyanix AI — Frontend Configuration
// Change the URL here when you deploy your backend to production (e.g., Azure or Render)

window.API_BASE = window.location.hostname === 'localhost' || window.location.hostname === '127.0.0.1'
  ? ''
  : 'https://gyanix-ai-backend.onrender.com'; // <-- REPLACE THIS WITH YOUR LIVE BACKEND URL IN PRODUCTION
